services:
  - type: web
    name: vroom
    runtime: docker
    plan: free
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: DATABASE_URL
        fromDatabase:
          name: vroom-db
          property: connectionString

databases:
  - name: vroom-db
    plan: free
